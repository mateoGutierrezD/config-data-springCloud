server:
  port: 9010
eureka:
  instance:
    hostname: localhost
  cliente:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}